export type TInfo = {
  country: string;
  capital: string;
  population: string;
  area: string;
  region: string;
  srcImg: string;
  borders: string;
};

export type TName = {
  name: string;
  onClick?: VoidFunction;
};
